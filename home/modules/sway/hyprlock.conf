# sample hyprlock.conf
# for more configuration options, refer https://wiki.hyprland.org/Hypr-Ecosystem/hyprlock
#
# rendered text in all widgets supports pango markup (e.g. <b> or <i> tags)
# ref. https://wiki.hyprland.org/Hypr-Ecosystem/hyprlock/#general-remarks
#
# shortcuts to clear password buffer: ESC, Ctrl+U, Ctrl+Backspace
#
# you can get started by copying this config to ~/.config/hypr/hyprlock.conf
#

$font = Fraunces 72pt SuperSoft

general {
    hide_cursor = false
}

# uncomment to enable fingerprint authentication
auth {
    fingerprint {
        enabled = true
        ready_message = Scan fingerprint to unlock
        present_message = Scanning...
        retry_delay = 250 # in milliseconds
    }
}

animations {
    enabled = true
    bezier = linear, 1, 1, 0, 0
    animation = fadeIn, 1, 5, linear
    animation = fadeOut, 1, 2, linear
    animation = inputFieldDots, 1, 1, linear
}

background {
    monitor =
    path = ~/Pictures/wall/darklichen.jpg
    blur_passes = 3
    blur_size = 4
    brightness = 0.25
}

input-field {
    monitor =
    size = 600, 110
    outline_thickness = 5
    inner_color = rgba(0a0a0acc)

    outer_color = rgba(3a625bff)
    check_color = rgba(ccccccff)
    fail_color = rgba(b38375ff)

    font_color = rgba(ccccccff)
    fade_on_empty = false
    rounding = 3

    font_family = $font
    placeholder_text = Password...
    fail_text = $PAMFAIL$FPRINTFAIL

    # uncomment if you wish to display a message during authentication
    check_text = Authenticating...

    # uncomment to use a letter instead of a dot to indicate the typed password
    # dots_text_format = *
    # dots_size = 0.4
    dots_spacing = 0.3

    # uncomment to use an input indicator that does not show the password length (similar to swaylock's input indicator)
    # hide_input = true

    position = 0, -60
    halign = center
    valign = center
}

# TIME
label {
    monitor =
    text = cmd[update:5000] date +"%l:%M %p"
    color = rgba(ccccccff)
    font_size = 100
    font_family = $font

    position = 0, 105
    halign = center
    valign = center
}

# DATE
label {
    monitor =
    text = cmd[update:60000] date +"%A, %B %d"
    color = rgba(ccccccff)
    font_size = 56
    font_family = $font

    position = 0, 240
    halign = center
    valign = center
}
